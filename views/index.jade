extends layout
block content
  p Welcome to #{title}
  #sensors
    - each s in sensors
      div.sensor
        div.title= s.title
        div.data(id=s.name+'_chart')
          script(type='text/javascript')
                init("#{s.name}");
